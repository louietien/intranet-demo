# Changelog

## v0.1.0

Initial public demo release — forked from a real internal company intranet, stripped of
all company-specific branding and integrations (Azure/MSAL login, PocketBase, Microsoft
Graph, Monday.com, Slack), and rebuilt on a fully client-side mock data layer so it can
run as a static export with no backend at all.
