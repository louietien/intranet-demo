export const APP_VERSION = '0.1.0'

export const CHANGELOG_MD = `
## v0.1.0 — Initial demo release

### About this build
This is a public, non-branded demo fork of a real internal company intranet. It ships with:

- News feed, comments, and reactions
- Knowledge base with a markdown editor
- People directory and org chart
- Shared files, personal notes, and a team calendar
- Dark mode, maintenance mode, and a ⌘K command palette

There is no real backend — every page is seeded with fictional data and stored only in
your browser (localStorage), so feel free to click around and try things out.
`.trim()
